# Sample artifacts — Hot Work Permits & Fire Watch

Four artifacts, one safety topic, generated from the personalized skills as they stand today.

| File | Skill | Method | Notes |
|---|---|---|---|
| `Hot Work Permit and Fire Watch Procedure.docx` | docx | **Walk-and-replace fallback path** | All embedded images (logo in header, cover graphic, watermark) preserved by the binary clone. Three content sections (`PERMIT REQUIREMENTS`, `FIRE WATCH RESPONSIBILITIES`, `PERMIT SUSPENSION AND EMERGENCY RESPONSE`) — the third was generated by cloning the second's heading+intro+bullets block. All five tables filled. Retains the template's 26 Revision History rows (most blank). |
| `Hot Work Procedure (docxtpl).docx` | docx | **Production docxtpl path** (preferred) | Rendered via `skills/docx/scripts/render_docx.py` from `Procedure Template (Jinja).docx` + `hotwork_input.json`. Smoke-test confirms 36/36 paragraphs identical to the walk-and-replace output, with cleaner table-row trimming (Revision History = 2 rows instead of 26). |
| `Hot Work Safety Moment.pptx` | pptx | Template-clone of `NextDecade Power Point Slide Master_Final_Oct 2025.potx` (color-corrected) | 3 slides: Cover (uses `Custom Layout` with tagline), Content (uses `ND Single Narrative 18 Font` with 6 bullets), Back cover (uses `23_Custom Layout` with tagline + URL). Brand orange `#FC7134` is now correct in accent2. |
| `Hot Work All-Hands Email.docx` | internal-comms | New .docx built per Writing Style Guide §5.4 | Segoe UI throughout. Headlines 20pt navy, subheads 16pt navy, body 11pt black. Casual-internal voice, contractions, "we / us / our". Carries the Confidential / Internal Use footer. |
| `Hot Work Permit Tracker.xlsx` | xlsx | Built using personalized xlsx skill brand chrome | 3 sheets: Permit Log (color-coded status), Permit Detail (single-permit close-out), Trend Summary (with totals row). Segoe UI everywhere, navy header rows, status fills (green = Closed, orange = Active, red = Suspended), classification footer on every page. |

## On pictures and graphics

- For `.docx` (Procedure): the cloned template carries the NextDecade logo embedded in the header, the cover graphic, and the watermark. The clone preserves all of these byte-identically.
- For `.pptx` (Safety Moment): all 32 media assets in the slide master come along by clone. Layouts already contain the brand chrome. Only text in placeholders was replaced.
- For `.xlsx`: there are no images by default. If you want a header logo on every page, drop the logo PNG from `uploads/04-document-templates/Procedure Template.docx → word/media/image2.png` into the worksheet header.

## Topic

**Hot work** is the highest-risk routine activity at any LNG facility. The procedure, safety moment, all-hands email, and tracker are all consistent with each other and reference the same fictional near-miss event (HW-2026-0143 on 14-Apr-2026).
